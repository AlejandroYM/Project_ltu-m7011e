apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chef-match-ingress
  namespace: todo-app
  annotations:
    kubernetes.io/ingress.class: traefik
    # Esta anotación fuerza a Traefik a dar prioridad máxima a este objeto
    traefik.ingress.kubernetes.io/router.priority: "10000"
spec:
  ingressClassName: traefik
  rules:
  - host: ltu-m7011e-5.se
    http:
      paths:
      - path: /recipes
        pathType: PathPrefix
        backend:
          service:
            name: recipe-service
            port:
              number: 8000
      - path: /users
        pathType: PathPrefix
        backend:
          service:
            name: user-service
            port:
              number: 8000
      - path: /recommendations
        pathType: PathPrefix
        backend:
          service:
            name: recommendation-service
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: chef-frontend
            port:
              number: 80